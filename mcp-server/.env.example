DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/ott_platform
OTT_BACKEND_PATH=../backend

# Transport: "stdio" (default, for Claude Code/Desktop) or "sse" (HTTP server, for Docker/AgentHub)
MCP_TRANSPORT=stdio
MCP_HOST=0.0.0.0
MCP_PORT=8080
